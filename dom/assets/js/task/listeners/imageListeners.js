function handleImageError(e) {
  // e.target.src = '../img/tasks/user-placeholder.png'
  // e.target.src = '/assets/img/tasks/user-placeholder.png'

  // img.alt = `${user.firstName[0]} ${user.lastName[0]}`
  e.target.remove();

}